# Claim Ledger: Hermes Agent v0.18.2

**Artifact ID:** `field-landscape-case-hermes-agent-v0-18-2`

**Status:** Canonical ledger for a maintainer-reviewed implementation case under D-012

**Canonical implementation:** `NousResearch/hermes-agent` annotated tag `v2026.7.7.2`, tag object `b7751df34688835a108e0d630f3495fc11f3df79`, dereferenced commit `9de9c25f620ff7f1ce0fd5457d596052d5159596`, package version `0.18.2`

**Audit status:** Complete event projection passes the fixed `provisional-promotion` profile with 24 declarations, 34 mappings, and 34 primary verifications; independent review and Gate A maintainer disposition are complete

| ID | Epistemic kind | Canonical claim | Evidence and exact locations | Confidence and transfer limits |
| --- | --- | --- | --- | --- |
| <a id="c001"></a>C001 | verified implementation fact | The pinned repository exposes several ingress surfaces over a shared `AIAgent` runtime. This case traces the classic `hermes chat` path from `hermes_cli.main.cmd_chat` through `cli.HermesCLI`, `AIAgent.run_conversation`, `agent.conversation_loop.run_conversation`, tool execution, turn finalization, and session persistence. | CP2-S009: `pyproject.toml` 308; `hermes_cli/main.py` 2191–2397; `cli.py` 11990–12312, 15669–15867; `run_agent.py` 393–614, 5745–5768; `agent/conversation_loop.py` 523–5308 | High for the pinned classic CLI path. Gateway, ACP, cron, batch, and alternate provider runtimes received targeted or survey treatment only. |
| <a id="c002"></a>C002 | verified implementation fact | Lifecycle is nested rather than singular: process and CLI session own a sequence of user turns; each turn owns model iterations and tool calls; compression, delegated children, persistent goals, background reviews, gateway sessions, cron jobs, and batch samples introduce additional boundaries with different persistence and completion semantics. The plugin hook named `on_session_end` fires after every `run_conversation` turn, whereas memory-provider and context-engine shutdown occur at actual session boundaries. | CP2-S009: `hermes_cli/main.py` 2191–2397; `agent/turn_context.py` 173–565; `agent/conversation_loop.py` 523–689; `agent/turn_finalizer.py` 454–505; `run_agent.py` 3273–3297; `cli.py` 1057–1134; `tools/delegate_tool.py` 1196–1397; `hermes_cli/goals.py` 1077–1600; `gateway/session.py` 147–203, 850–1017; `cron/scheduler.py` 2387–2660; `batch_runner.py` 244–397 | High for the named boundaries. Not every ingress was traced end to end. |
| <a id="c003"></a>C003 | verified implementation fact | Hermes constructs and caches a system prompt from identity, task guidance, project instructions, a filtered skill index, memory/profile snapshots, environment, and provider/model metadata. Each call then adds the current history projection, ephemeral plugin and external-memory context, tool schemas, prefill, and provider-specific normalization. | CP2-S009: `agent/system_prompt.py` 113–505; `agent/prompt_builder.py` 1172–1220, 1417–1683, 1827–1967; `agent/turn_context.py` 271–515; `agent/conversation_loop.py` 750–952; `model_tools.py` 357–567 | High for the pinned path. Exact prompts and capabilities remain configuration-, provider-, plugin-, project-, and session-dependent. The R2/R3 interpretation belongs to C020. |
| <a id="c004"></a>C004 | inference | Memory, profile, date, and related material labeled volatile during prompt construction normally become session snapshots because the complete system prompt is cached until explicit invalidation or compression rebuilds it. Mid-session durable writes do not automatically refresh the already cached prompt. | CP2-S009: `agent/system_prompt.py` 113–130, 423–505; `agent/turn_context.py` 330–335; `agent/conversation_compression.py` 669–710; `cli.py` 7012–7013, 7264–7269; `tools/memory_tool.py` 113–205, 280–386; `agent/background_review.py` 700–776 | Moderate-high for the inspected cache path. Plugins, explicit CLI invalidation, or alternate context engines may introduce other refresh behavior. |
| <a id="c005"></a>C005 | verified implementation fact | The normal loop is model-directed over tool calls and text completion inside a programmed envelope that owns iteration budgets, interrupts, provider restoration/fallback, retries, context-pressure compression, malformed-output recovery, tool-name and argument validation, persistence ordering, verification nudges, and finalization. Known-truncated tool arguments fail closed without execution. | CP2-S009: `agent/conversation_loop.py` 523–689, 954–1251, 4020–4711, 4786–5308; `agent/agent_runtime_helpers.py` 1138–1296; `agent/turn_finalizer.py` 53–133; targeted loop and truncation tests | High for the inspected loop. Runtime integrity does not establish semantic task success. |
| <a id="c006"></a>C006 | verified implementation fact | Tool mediation has distinct authority and observation stages: visible schemas come from filtered runtime definitions; middleware, plugins, approvals, guardrails, and optional checkpoints can alter or block a request; execution returns a raw result; post-processing can add guardrail guidance, transform or spill large results, add retrieval hints, normalize multimodal content, and only then append the model-facing tool observation. | CP2-S009: `model_tools.py` 357–567, 1020–1346; `hermes_cli/plugins.py` 2099–2298; `agent/tool_executor.py` 306–943, 965–1624; `run_agent.py` 5631–5652 | High for built-in and registry dispatch paths. Installed plugins and middleware remain trusted extension code and can change semantics. |
| <a id="c007"></a>C007 | verified implementation fact | SQLite is the canonical session/message store on the traced path. Inbound user content is persisted before the model call, assistant tool calls are persisted before tool side effects, and results are persisted after execution. Fresh built-in configuration enables in-place compression: old rows are soft-archived while a summarized active projection is inserted under the same session ID; legacy rotation remains supported. | CP2-S009: `agent/turn_context.py` 329–348; `agent/conversation_loop.py` 4597–4688; `agent/tool_executor.py` 1587–1592; `run_agent.py` 1726–1885; `hermes_state.py` 700–952, 3406–3513, 3659–3724; `hermes_cli/config.py` 1342–1404; `agent/conversation_compression.py` 500–880 | High for the pinned fresh-default path. User config and upgraded installations may retain different settings; JSON snapshots are optional. |
| <a id="c008"></a>C008 | verified implementation fact | Built-in context compression uses a lossy active projection with protected head and recent tail, tool-output pruning, and an LLM-produced structured summary. Depending on error class and configuration, summary failure can abort or use a deterministic placeholder while dropping middle turns; repeated compression is explicitly warned as quality-degrading. | CP2-S009: `agent/context_compressor.py` 699–708, 2707–3041; `agent/conversation_compression.py` 631–750, 915–950; `hermes_cli/config.py` 1342–1364 | High for mechanics. Semantic retention and task impact were not behaviorally evaluated in this case. |
| <a id="c009"></a>C009 | verified implementation fact | Built-in durable memory is bounded `MEMORY.md` and `USER.md` state, enabled by fresh defaults with writes not approval-gated. At most one optional external memory provider can add retrieval and asynchronous turn synchronization; committed built-in writes can be mirrored, while staged or failed writes are excluded. | CP2-S009: `hermes_cli/config.py` 2100–2124; `tools/memory_tool.py` 1–24, 113–205, 280–386; `agent/memory_manager.py` 353–666, 879–988 | High for the built-in and provider-manager boundaries. External-provider internals and deployment-specific memory quality remain outside the case. |
| <a id="c010"></a>C010 | verified implementation fact | Top-level model delegation is forced into asynchronous background execution, while orchestrator-child delegation is synchronous. Children receive fresh budgets and sessions, inherit no broader capability than the parent, can use configured model/provider overrides, and return bounded summaries; fresh defaults allow three concurrent children, depth one, 50 iterations, no wall-clock timeout, and noninteractive denial of dangerous-command approvals. | CP2-S009: `hermes_cli/config.py` 2126–2188; `tools/delegate_tool.py` 1–17, 661–735, 1044–1397, 1544–1621, 2342–2888; `run_agent.py` 5654–5684; delegation tests | High for orchestration mechanics. No causal quality or cost-benefit study of Hermes delegation was run. |
| <a id="c011"></a>C011 | verified implementation fact | Ordinary text completion can stop the main loop without a general independent task judge. A separate verification-on-stop policy can suppress premature completion after code-like edits when fresh passing execution evidence is absent and ask the same agent for bounded follow-up verification. A missing config file is treated as current and inherits surface-aware `auto`; an existing versionless file is treated as legacy, and an unset or `auto` value is written as `false` when migration runs, as it is for affected older versioned installations. Thus “the default” depends on configuration origin and migration history. | CP2-S009: `agent/verification_stop.py` 1–6, 75–170, 245–310; `agent/conversation_loop.py` 5128–5224; `hermes_cli/config.py` 1031–1037, 5082–5122, 5876–5932; `tests/agent/test_verification_stop.py` 47–170 | High for policy and configuration-generation behavior. The nudge does not itself execute tests or independently judge arbitrary tasks. |
| <a id="c012"></a>C012 | verified implementation fact | Optional persistent `/goal` mode wraps ordinary turns in an auxiliary judge loop that can use structured verification criteria, user-added subgoals, and live background-process state. A done verdict stops; continue re-enters the same session; wait parks the loop; API failures continue fail-open; repeated parse failures and the 20-turn fresh default pause rather than loop indefinitely. | CP2-S009: `hermes_cli/goals.py` 113–218, 294–475, 836–964, 1077–1600; `hermes_cli/config.py` 2196–2210; `cli.py` 9026–9213; goal tests | High for optional mechanics. Judge reliability and task benefit are unmeasured, and ordinary chat is not goal mode. |
| <a id="c013"></a>C013 | verified implementation fact | Plugins and middleware are cross-cutting injection surfaces rather than a single subsystem: they can observe or change model requests, ephemeral context, tool requests, approvals, execution, tool results, verification, sessions, subagents, and gateway events. Some security-sensitive approval paths fail closed, while many observation/transformation hook failures are isolated or fail open to preserve availability. | CP2-S009: `hermes_cli/plugins.py` 135–213, 225–268, 1277–1455, 1890–1956, 2099–2298; `hermes_cli/middleware.py` 77–210; `agent/shell_hooks.py` 203–284; `gateway/hooks.py` 52–160 | High for the hook vocabulary and inspected call sites. Third-party plugin behavior remains unknowable. |
| <a id="c014"></a>C014 | verified implementation fact | Hermes performs periodic across-run adaptation: every ten eligible turns or tool iterations, a best-effort background fork can update durable memory or skills using conversation content, corrections, preferences, and observed techniques. The fork is isolated from canonical session persistence and external memory providers, restricted to memory/skill tools, and uses the main model by default; successful writes can apply without human approval under fresh defaults. | CP2-S009: `agent/agent_init.py` 1326–1347, 1422–1428; `agent/turn_context.py` 278–314; `agent/turn_finalizer.py` 454–480; `agent/background_review.py` 1–17, 160–325, 607–920; `hermes_cli/config.py` 1637–1655, 2100–2116, 2269–2280; background-review tests | High for the mechanism and defaults. Review quality, correctness of learned artifacts, and downstream outcome impact are not established. |
| <a id="c015"></a>C015 | verified implementation fact | The enabled-by-default curator normally performs deterministic usage/recency maintenance and recoverable archival; LLM consolidation is disabled by default. `/learn` is an ordinary prompted agent turn that asks the model to research and create one skill, and the learning graph visualizes stored artifacts, usage metadata, and lexical overlap. None is a measured outcome-optimization loop. | CP2-S009: `hermes_cli/config.py` 2283–2331; `agent/curator.py` 56–64, 178–369, 403–554, 1480–1735; `agent/learn_prompt.py` 99–150; `agent/learning_graph.py` 125–153, 193–323; curator and learn tests | High for observed mechanisms; high-moderate for bounded absence of an outcome objective in these paths. Private or downstream optimization remains unknowable. |
| <a id="c016"></a>C016 | verified implementation fact | The pinned implementation records session messages, tool traces, token and cost usage, model/provider state, activity, and optional plugin telemetry; batch mode can record trajectories. These observability surfaces do not by themselves score task success or feed outcomes back into automatic policy optimization. | CP2-S009: `agent/conversation_loop.py` 2089–2246; `hermes_state.py` 700–952; `batch_runner.py` 244–397, 527–638; `plugins/observability/langfuse/`; relevant observability tests | High for present mechanisms and bounded negative claim. Private evaluation systems and external analysis remain unknown. |
| <a id="c017"></a>C017 | inference | Hermes and the reviewed OpenClaw repository have distinct recorded Git roots. Hermes later added an explicit OpenClaw migration, an OpenClaw-inspired permissions change, an OpenClaw-modeled subagent prompt, and a named port of an OpenClaw streaming fix. This establishes migration compatibility and selective mechanism-level influence, not whole-repository fork descent or independent convergence. | CP2-S009: roots and commits `d53035ad821f7ba80c9f74637d267064837cb8d3`, `0ce190be0dd7b0d6e0b9ccc59f6cfc372b1cd835`, `b16f9d438ba18cb433a94a47dd99a05abc808d0a`; `hermes_cli/claw.py` 313–555; `tools/delegate_tool.py` 661–736; `gateway/stream_consumer.py` 1287–1307. CP2-S005: reviewed OpenClaw root and pin. | High for named evidence; low for any broader ancestry inference. Similarity must be assessed mechanism by mechanism. |
| <a id="c018"></a>C018 | inference | Reachable code, comments, tests, and selected history show substantial engineering devoted to runtime and recovery invariants—prompt/session isolation, persistence-before-effects, malformed-call handling, compaction, delegated-child bounds, and background-review containment—while task acceptance remains narrower and surface-dependent. The evidence identifies concrete failure surfaces, not their frequency or relative cost. | CP2-S009: E11 and E19; `agent/conversation_loop.py` 4020–4711; `agent/turn_finalizer.py` 454–505; `agent/background_review.py` 607–920; `agent/verification_stop.py` 75–170, 245–310; `hermes_cli/goals.py` 1077–1600; `tests/agent/test_verification_stop.py` 47–170; selected commits `e2fa509bf3d63d026cbe6e2a34c13fd913526eb9`, `24add1db743dec5166048d406d8b8a58555b7697`, `2f1a47b90e60914b61d361f7c4147a11e7ebebbc`, `4c2961c511c523c621b8847493f032ab65bf116a`, `7bbffceb9c35f74039ddc1eecd3fc301f35fee46` | Moderate-high for the allocation inference; no incidence, effort accounting, or outcome experiment was performed. |
| <a id="c019"></a>C019 | source-reported claim | External evidence qualifies rather than validates Hermes's mechanisms: recent compaction preprints report loss, variance, and latency under other protocols; BenchAgent reports that multi-agent workflows can underperform a matched single-agent anchor; MemoryAgentBench separates memory competencies and finds no evaluated method masters all; self-correction reviews favor reliable external feedback; production evaluation guidance separates trajectories from outcomes. | PI-S002, PI-S003, FL-S006, FL-S008, FL-S009, FL-S002, contextualized by CP2-S009 | Moderate-low to moderate transfer confidence. None directly evaluates Hermes v0.18.2, its prompts, its background review, its goal judge, or its delegated children. |
| <a id="c020"></a>C020 | hypothesis | Hermes observes TF-001 through TF-007 and adds two candidate cross-cutting questions: configuration origin and generation can make no-file fresh, versionless existing, versioned migrated, explicitly configured, and runtime-effective values diverge; lineage can vary by mechanism inside one system relationship. These are Checkpoint 3 inputs, not approved taxonomy changes. | CP2-S009: E03–E06 and E09–E21; `hermes_cli/config.py` 1031–1037, 1342–1404, 5082–5122, 5876–5932; recorded Hermes root and commits `d53035ad821f7ba80c9f74637d267064837cb8d3`, `0ce190be0dd7b0d6e0b9ccc59f6cfc372b1cd835`, and `b16f9d438ba18cb433a94a47dd99a05abc808d0a`; `research/TAXONOMY-FRICTION.md` Hermes rows for TF-001–TF-007. Reviewed CP2-S001, CP2-S002, and CP2-S005 provide prior friction context. | Moderate for Hermes observations; low-to-moderate for general taxonomy implications until four-case consolidation. |
| <a id="c021"></a>C021 | open question | The concurrent registry-tool path may traverse execution middleware at both the agent wrapper and registry dispatcher, while the sequential path appears to traverse only the inner wrapper. Static inspection did not establish whether middleware actually executes twice, and no authorized behavioral check was run. | CP2-S009: `agent/agent_runtime_helpers.py` 2265–2293; `model_tools.py` 1273–1285; `tests/run_agent/test_run_agent.py` 3098–3179; `tests/hermes_cli/test_plugins.py` 135–151, 192–297 | Low. Treat as a focused test candidate, not a defect or frequency claim. |
| <a id="c022"></a>C022 | verified implementation fact | Fresh configuration selects the local terminal backend. Foreground commands execute through a `LocalEnvironment` that spawns shell processes directly on the host, and local background commands use the host process registry; Docker, Singularity, Modal, Daytona, and SSH are configurable alternatives. Approvals, guardrails, safe mode, working-directory validation, and credential filtering are policy or exposure controls, not host-process isolation. | CP2-S009: `hermes_cli/config.py` 1093–1107; `tools/terminal_tool.py` 1256–1378, 1390–1455, 2273–2331, 2360–2388, 2607–2635; `tools/environments/local.py` 94–227, 922–1047; `tools/process_registry.py` 682–772 | High for the fresh default and built-in dispatch paths. User configuration and deployment can select isolated or remote backends; no security assurance or behavioral containment test was performed. |
| <a id="c023"></a>C023 | inference | Explicit OpenClaw migration can reduce operator conversion work, but it does not establish semantic equivalence where mapped concepts have different lifecycle, context, authorization, or persistence contracts. Unsupported concepts are archived for manual handling. | CP2-S009: `README.md` 186–212; `website/docs/guides/migrate-from-openclaw.md` 7–184; `hermes_cli/claw.py` 313–555 | Moderate for the transfer-risk inference. No migration-success, semantic-equivalence, switching-cost, or outcome study was inspected. |
| <a id="c024"></a>C024 | verified implementation fact | The classic CLI lazily resolves model, provider, API mode, credentials, and an eligible provider-scoped credential pool per turn, rebuilding the agent when the route signature changes. Recovery can rotate credentials within one provider or activate an ordered cross-provider/model fallback; each later turn normally attempts to restore the primary runtime unless cooldown remains active. Request fields are rebuilt for the active transport on each attempt. | CP2-S009: `cli.py` 3783–3938, 11990–12035; `hermes_cli/cli_agent_setup_mixin.py` 25–216, 332–378; `hermes_cli/runtime_provider.py` 392–532, 1509–1742; `agent/credential_pool.py` 1369–1568; `agent/agent_runtime_helpers.py` 696–956, 1138–1296; `agent/chat_completion_helpers.py` 666–935, 1232–1561; `agent/conversation_loop.py` 1151–1324 | High for the classic CLI/shared `AIAgent` route. Gateways, TUI, ACP, custom providers, configured fallbacks, and credential availability can change the effective route. |

## Prose-to-ledger attestation

Every material prose claim in the case study is linked above or framed as a boundary, limitation, or open question. Canonical mappings, primary verification, the ledger digest, attestation, and review scope are recorded in `research/provenance/hermes-agent-case-events.jsonl` and projected in the generated audit. Implementation presence, activity, and lineage are never used as effectiveness evidence.

## Review scope

The ledger covers the classic CLI control path, targeted alternate ingress boundaries, model/prompt/context construction, tool and observation mediation, persistence and compression, memory, delegation, recovery and verification, goals, extensions, observability, adaptation, the bounded OpenClaw relationship, research comparison, and taxonomy friction. It excludes hosted services, exhaustive gateways/providers/tools, third-party plugins, benchmark reproduction, upstream test execution, private deployment behavior, security assurance, and effectiveness claims not supported by external outcome evidence.
