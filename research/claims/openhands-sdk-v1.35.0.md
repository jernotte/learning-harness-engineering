# Claim Ledger: OpenHands Software Agent SDK v1.35.0

**Artifact ID:** `field-landscape-case-openhands-sdk-v1-35-0`

**Status:** Canonical ledger for maintainer-reviewed case

**Canonical implementation:** `OpenHands/software-agent-sdk` v1.35.0 at `9028562e2d5eda76de662ec9b7584125760eb83f`

**Audit status:** Complete — 17 declarations, 28 exact mappings, and 28 primary verifications; promotion-profile audit passes

| ID | Epistemic kind | Canonical claim | Evidence and exact locations | Confidence and transfer limits |
| --- | --- | --- | --- | --- |
| <a id="c001"></a>C001 | verified implementation fact | The pinned repository composes SDK, tools, workspace, and agent-server packages; `Conversation` selects local or remote execution, while remote execution ultimately reconstructs and runs a `LocalConversation` on the server. | CP2-S002: repository root and package manifests; `conversation/conversation.py`; `conversation/impl/remote_conversation.py`; `agent_server/conversation_service.py`; `tool/tool.py` 133–155 | High for v1.35.0. Remote backend and hosted deployment internals were only surveyed. |
| <a id="c002"></a>C002 | verified implementation fact | `ConversationState` keeps mutable metadata and an append-only tree event log separately from an active-branch, condensation-aware `View` containing only model-convertible events; persistence, replay, navigation, and view rebuilding preserve that distinction. | CP2-S002: `conversation/state.py`; `event_store.py`; `context/view/view.py` 22–159; view property modules; tree/view/persistence tests | High for the pinned local implementation. Durability requires file-backed persistence. |
| <a id="c003"></a>C003 | verified implementation fact | Lifecycle control is nested: `LocalConversation` owns ingress, status, run/step, pause, interrupt, limits, and cancellation, while the agent server adds leases, atomic background-run ownership, concurrency bounds, event publication, rerun handling, and crash restoration. | CP2-S002: `local_conversation.py` 145–260, 1628–1722, 1724–2333, 2390–2495; `agent_server/event_service.py` 720–1055; `conversation_service.py` 700–861, 1305–1357; representative lifecycle tests | High for traced paths. ACP-specific internals and all endpoints were not analyzed. |
| <a id="c004"></a>C004 | verified implementation fact | A frozen agent configuration separates model/prompt policy from durable conversation state, while initialization and each call materialize runtime tools, skills, plugins, system instructions, active view, and call context; explicit plugins are pinned but ambient plugins can be rediscovered unpinned on resume. | CP2-S002: `agent/base.py` 96–379, 399–604, 647–713; `agent/agent.py` 419–522; `local_conversation.py` 880–1428; plugin and path-rule tests | High for mechanics. Exact deployed context remains configuration- and external-plugin-dependent. |
| <a id="c005"></a>C005 | verified implementation fact | The normal loop is model-directed over actions, while deterministic code owns pending-action completion, sampling boundaries, dispatch, confirmation, budgets, recovery, and status; assistant content or a `FinishAction` can finish an ordinary run without independent task acceptance. | CP2-S002: `agent/agent.py` 613–982; `agent/response_dispatch.py`; `local_conversation.py` 1724–2333; finish and status-transition tests | High for the ordinary configured path. Stop hooks, critics, goal mode, and custom agents may alter behavior. |
| <a id="c006"></a>C006 | verified implementation fact | Model tool calls are normalized and validated into typed actions, may pass through risk analysis, confirmation and synchronous blocking pre-hooks, execute through a runtime tool, and return typed model-visible observations or errors; async pre-hooks cannot block, post-hooks cannot undo effects, and direct `execute_tool()` intentionally bypasses loop authorization safeguards. | CP2-S002: `agent/agent.py` 991–1349; `tool/tool.py` 197–389; `hooks/conversation_hooks.py` 105–241; `hooks/manager.py` 70–120; `local_conversation.py` 2809–2850; tool and confirmation tests | High for mechanics. Custom executors and async hooks remain inside the caller/plugin trust boundary. |
| <a id="c007"></a>C007 | verified implementation fact | Tool concurrency defaults to one; when raised, calls share conversation/workspace state and rely on declared resource keys, tool-wide fallback locks, or an explicit no-lock declaration for safe scheduling. | CP2-S002: `agent/base.py` concurrency field; `agent/parallel_executor.py` 1–300; `tool/tool.py` 120–129, 337–345; terminal resource declaration 268–330; concurrency tests | High for scheduler policy. No outcome or race-frequency experiment was run. |
| <a id="c008"></a>C008 | verified implementation fact | `LocalWorkspace` executes directly on the host; containment is optional and supplied by remote/container workspace deployment rather than by the local agent loop itself. | CP2-S002: `workspace/base.py` 23–180; `workspace/local.py` 17–69; conversation factory and remote workspace interfaces; pinned README 30–43 | High for local behavior and supported boundary. Container-security effectiveness was not assessed. |
| <a id="c009"></a>C009 | verified implementation fact | Condensation is optional on `AgentBase`; the direct README quick start has none, while `get_default_agent()` installs an LLM summarizer that keeps a prefix and suffix, records forgotten event IDs and summary, and distinguishes hard token/request pressure from soft event-count pressure. | CP2-S002: `agent/base.py` 250–266; `tools/preset/default.py` 68–89; `context/condenser/llm_summarizing_condenser.py` 38–291; `context/condenser/base.py` 95–230; condenser tests | High for configured behavior. Semantic retention and task effects are not established by implementation. |
| <a id="c010"></a>C010 | verified implementation fact | Optional delegation is implemented as a tool that spawns separate local conversations, shares the parent workspace path, runs child tasks concurrently, inherits or overrides confirmation policy, aggregates final text, and rolls child usage into parent metrics. | CP2-S002: `tools/delegate/definition.py`; `tools/delegate/impl.py` 33–409; subagent registry and tests | High for the inspected delegate tool. It is not default core scheduling and does not provide independent environment isolation or acceptance. |
| <a id="c011"></a>C011 | inference | Recovery and lifecycle integrity are major engineering concerns in the pinned design: unmatched-action repair, view enforcement, stuck detection, leases, and targeted pre-release fixes address concrete failures, but this evidence does not measure incidence or task correctness. | CP2-S002: recovery paths and tests; commits `27cb289b`, `41169ac4`, `62ed727a`, `0aefe365`, `3da1d54d`; `stuck_detector.py` 24–274 | Moderate. Concrete defects and mechanisms are verified; relative engineering priority and generality are inferred. |
| <a id="c012"></a>C012 | verified implementation fact | Independent task checking is optional: the critic defaults to absent and fails open on evaluator exceptions, while `/goal` is a separate transcript-judge loop whose parse failure conservatively continues; neither governs ordinary `Conversation.run()` by default. | CP2-S002: `agent/base.py` critic field; `agent/critic_mixin.py` 25–138; `conversation/goal/controller.py` 78–133; `goal/judge.py` 43–120; agent-server goal path and tests | High for default/configured mechanics. Judge accuracy and real-world benefit are unmeasured here. |
| <a id="c013"></a>C013 | verified implementation fact | The bounded implementation records events, model completions, token/cost metrics, stats, traces, and server streams, but no inspected mechanism converts cross-run outcomes into automatic prompt, tool, routing, or policy optimization. | CP2-S002: event hierarchy; `llm/utils/telemetry.py`; `llm/llm.py` telemetry paths; server log/stats callbacks; observability modules; bounded repository search | High for present observability and bounded absence in the selected path. Downstream or private adaptation remains unknowable. |
| <a id="c014"></a>C014 | source-reported claim | The OpenHands SDK technical report reports a 61% lower system-attributable error rate for V1 versus V0 during a 15-day parallel rollout and low event-log persistence/replay overhead on 39,870 events; these results support the redesign as a bundle, not any single mechanism or this tag in isolation. | OH-S001: §5.1, lines 450–463; §5.2, lines 464–473; architecture/design sections | Moderate-high for the reported protocol and numbers; author-produced comparison, bundled redesign, and version-family limits constrain causal transfer. |
| <a id="c015"></a>C015 | source-reported claim | Existing evidence qualifies rather than validates OpenHands's optional summarization and judge layers: recent compaction preprints report loss/latency/variance under other protocols, Kamoi et al. favor reliable external feedback over ungrounded intrinsic correction, and production evaluation guidance separates trajectory from outcome grading. | PI-S002, PI-S003, FL-S009, FL-S002, contextualized by CP2-S002 mechanisms | Moderate-low to moderate transfer confidence. None directly evaluates OpenHands v1.35.0's condenser, critic, or goal judge. |
| <a id="c016"></a>C016 | hypothesis | Across Pi and OpenHands, lifecycle scale, R2/R3/R5 coupling, R5/R6 output shaping, and cross-cutting extensions recur; OpenHands additionally suggests that R9 needs an explicit distinction between runtime integrity/recovery and task-success verification. | CP2-S002 case-wide evidence and reviewed Pi friction record | Low until more cases test counterexamples. This is taxonomy friction, not an approved revision. |
| <a id="c017"></a>C017 | source-reported claim | SWE-agent's historical experiments report that agent-computer interface choices and associated ablations affected coding outcomes, supporting the relevance of action/observation design without validating OpenHands's exact typed interface or current-model behavior. | FL-S016: abstract, ACI design sections, and retained ablation evidence; contextualized by CP2-S002 action/observation mechanisms | Moderate transfer confidence across different implementations, models, benchmarks, and dates. |

## Prose-to-ledger attestation

Every material prose claim in the case study is linked above or framed as a question, boundary statement, or limitation. Canonical mappings, primary verification, the ledger digest, attestation, and review scope are recorded in `research/provenance/openhands-sdk-case-events.jsonl` and projected in the generated audit. No implementation presence is used as evidence of effectiveness.

## Review scope

The ledger covers the local conversation control path, targeted agent-server wrapper, model/context/tool construction, action and observation mediation, event persistence/view projection, condensation, delegation, verification/recovery, observability, research comparison, and taxonomy friction. It excludes the legacy application, hosted products, exhaustive tools/providers/backends, benchmark reproduction, live-doc projection, behavioral experiments, and security assurance.
