# Claim Ledger: Pi v0.80.6

**Artifact:** `research/case-studies/pi-v0.80.6.md`  
**Artifact ID:** `field-landscape-case-pi-v0-80-6`  
**Ledger owner:** Primary agent  
**Maturity:** Reviewed finding set — maintainer approved 2026-07-16  
**Review basis:** Direct maintainer review plus external line-level verification of representative load-bearing claims and independent audit regeneration  
**Prose-to-ledger attestation:** `prose-attestation-ed7fc7bc5bd891fd83`; digest `d4cb94aaf37e5984c3c9808209206f13315dd570cbec885f280eecd3545cba65`  
**Audit status:** Complete provenance; 15 declarations, 19 mappings, and 19 primary verifications; promotion-profile audit passes

Canonical claim IDs use the artifact ID plus the suffix below.

| ID | Epistemic kind | Canonical claim | Evidence and relationship | Confidence and limits |
| --- | --- | --- | --- | --- |
| <a id="c001"></a>C001 | verified implementation fact | At v0.80.6, Pi's coding path layers `pi-ai`, the low-level `Agent`, and `pi-coding-agent`'s `AgentSession`; the separate `AgentHarness` implementation is present but is not imported by coding-agent source and its pinned design document marks lifecycle work as provisional. | CP2-S001: repository package tree; `agent.ts`; `agent-session.ts`; `agent-harness.ts`; `agent-harness.md` | High for the pin; absence is bounded to inspected source at this commit, not future migration intent. |
| <a id="c002"></a>C002 | verified implementation fact | The low-level loop is model-directed over tool calls, with deterministic code owning event order, turn snapshots, queue-drain points, tool scheduling, termination hints, cancellation, and provider-failure normalization. | CP2-S001: `agent-loop.ts` 155–270, 413–585; `agent.ts` 424–570; tests 525–1305 | High for v0.80.6. No effectiveness implication. |
| <a id="c003"></a>C003 | verified implementation fact | Pi builds each coding prompt from active tools and guidelines, project/global instruction files, skills metadata, date, and cwd, while refreshing model, tools, system prompt, and thinking level between tool turns. | CP2-S001: `system-prompt.ts` 28–173; `resource-loader.ts` 85–119; `agent-session.ts` 473–499 and 983–1021 | High. Project configuration and extensions can materially change any concrete run. |
| <a id="c004"></a>C004 | verified implementation fact | Pi validates tool arguments, exposes pre/post extension hooks, defaults to parallel tool execution unless a tool or configuration forces sequential execution, bounds tool observations, and refuses to execute calls from a length-truncated assistant response; trusted pre-call hooks can mutate the validated argument object before execution. | CP2-S001: `agent-loop.ts` 383–747; `read.ts` 212–315; `bash.ts` 301–397; agent-loop tests 310–969 | High for mechanics. Trusted hooks are inside the trust boundary. |
| <a id="c005"></a>C005 | verified implementation fact | Pi preserves an append-only tree-shaped session separately from the compaction-aware active model context, enabling retained history and branching even when older entries leave the model view. | CP2-S001: `session-manager.ts` 414–465, 974–1050, 1187–1365; session branching/compaction tests | High for v0.80.6. Storage durability depends on persistent rather than in-memory mode. |
| <a id="c006"></a>C006 | verified implementation fact | Default coding-session compaction uses a model-generated structured summary plus a retained recent tail, triggered manually, by threshold, or by overflow; overflow may compact and retry once. | CP2-S001: `agent-session.ts` 1729–2050; `compaction.ts`; pinned compaction documentation and tests | High for implementation. Does not establish semantic retention or task benefit. |
| <a id="c007"></a>C007 | verified implementation fact | Delegation is not a primitive of the default core loop; the pinned repository demonstrates single, parallel, and chained subagents as an optional extension that spawns separate Pi processes. | CP2-S001: no subagent primitive in agent/coding core; `examples/extensions/subagent/index.ts` 1–700 | High for inspected boundary. Extensions make the set of possible deployed behaviors open-ended. |
| <a id="c008"></a>C008 | source-reported claim | Pi intentionally provides no built-in permission system or sandbox; project trust gates resource loading, while execution containment is assigned to extensions or external OS/virtualization boundaries. | CP2-S001: README 39–45; `docs/security.md` 3–59; `docs/containerization.md` | High for declared and code-consistent policy; no security-effectiveness claim. |
| <a id="c009"></a>C009 | verified implementation fact | Pi provides retry and context-overflow recovery for runtime failures, but the default inspected path has no independent task-success verifier, acceptance gate, or rollback policy. | CP2-S001: `agent-session.ts` 1027–1062, 1890–2050, 2574–2640; default tools and prompt | High for present/absent code at the pin. Extensions can add policies. |
| <a id="c010"></a>C010 | verified implementation fact | Pi records rich operational evidence—events, JSONL sessions, model/tool metadata, usage, cache and cost data, exports, and regression tests—but the inspected repository does not contain a task-level evaluation harness or pinned coding-outcome result for v0.80.6. | CP2-S001: event types, session entries, export/stat code, package tests and scripts; repository-wide evaluation search | High for inspected repository. External private evaluations remain unknowable. |
| <a id="c011"></a>C011 | inference | Pi's recent fixes for truncated tool arguments, stale next-turn state, and settled event timing indicate that action integrity and lifecycle boundaries are active failure surfaces rather than incidental implementation detail. | CP2-S001: commits `351efc82`, `e547bb9f`, `e9fa5a68` and regression tests | Moderate. Commit selection is illustrative, not a measured failure-frequency study. |
| <a id="c012"></a>C012 | source-reported claim | SWE-agent's historical experiments support the proposition that coding-agent action/observation interface design can materially affect outcomes, but do not validate Pi's exact interface or current models. | FL-S016 supports general proposition; CP2-S001 contextualizes Pi interface | Moderate transfer confidence; different implementation, model, benchmark, and date. |
| <a id="c013"></a>C013 | source-reported claim | Recent preprints report that synchronous LLM summarization compaction can be lossy, blocking, and variable, and propose deterministic or parallel alternatives; these results qualify rather than directly refute Pi's unmeasured default compaction. | PI-S002 contextualizes/opposes universal adequacy; PI-S003 contextualizes/opposes predictable low-cost compaction; CP2-S001 establishes Pi mechanism | Moderate-low: recent preprints, different tasks/models, and no direct Pi comparison. |
| <a id="c014"></a>C014 | source-reported claim | The Kamoi et al. survey found reliable external feedback substantially more credible for correction than prompted intrinsic self-feedback, qualifying any assumption that a model-directed coding loop will verify itself without an explicit policy. | FL-S009 contextualizes CP2-S001's default stopping and verification path | Moderate transfer confidence; survey predates current models and does not evaluate Pi. |
| <a id="c015"></a>C015 | hypothesis | Pi suggests the lens may need explicit nested lifecycle scales and a cross-cutting extension/policy-injection dimension, because session lifecycle and turn lifecycle differ while extensions span most responsibilities. | CP2-S001 case-wide friction | Low until contrasted with the remaining cases; no taxonomy change authorized. |

Every material prose claim in the case study is either linked above or framed as a question, boundary statement, or transition. Canonical mappings and primary verification live in `research/provenance/pi-pilot-events.jsonl`.
